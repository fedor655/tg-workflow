# подтягиватель Telegram
telethon>=1.44

# расшифровка речи (Whisper large-v3)
faster-whisper>=1.2

# системно, не через pip:
#   ffmpeg    — должен быть в PATH
#   CUDA      — опционально, для расшифровки на видеокарте (иначе CPU)
